spring:
  application:
    name: companies-service

  datasource:
    url: jdbc:postgresql://localhost:5432/companies
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081

logging:
  level:
    org:
      springframework: INFO
