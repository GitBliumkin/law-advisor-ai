spring:
  application:
    name: law

  datasource:
    url: jdbc:postgresql://localhost:5432/law
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8082

logging:
  level:
    org.springframework: INFO
