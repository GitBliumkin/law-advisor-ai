spring:
  application:
    name: laws

  datasource:
    url: jdbc:postgresql://localhost:5432/laws
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: update

qdrant:
  base-url: http://localhost:6333

server:
  port: 8082

logging:
  level:
    org:
      springframework: INFO