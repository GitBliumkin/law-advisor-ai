spring:
  application:
    name: qdrant

  datasource:
    url: jdbc:postgresql://localhost:5432/qdrant
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8083
  
qdrant:
  base-url: http://localhost:6333

logging:
  level:
    org:
      springframework: INFO
